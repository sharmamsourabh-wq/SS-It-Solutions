<UserControl x:Class="SolarQuotationBillingSystem.Views.QuotationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:vm="clr-namespace:SolarQuotationBillingSystem.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="900" d:DesignWidth="1200"
             TextElement.Foreground="{DynamicResource MaterialDesignBody}"
             TextElement.FontWeight="Regular"
             TextElement.FontSize="13"
             TextOptions.TextFormattingMode="Ideal"
             TextOptions.TextRenderingMode="Auto"
             Background="{DynamicResource MaterialDesignPaper}"
             FontFamily="Cambria">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/> <!-- Top Cards -->
                <RowDefinition Height="Auto"/> <!-- DataGrid -->
                <RowDefinition Height="Auto"/> <!-- Summary & Actions -->
            </Grid.RowDefinitions>

            <!-- TOP CARDS: Customer Info & Quotation Details -->
            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Left Card: Customer Information -->
                <GroupBox Grid.Column="0" Header="Customer Information" Style="{DynamicResource MaterialDesignCardGroupBox}" materialDesign:ColorZoneAssist.Mode="Standard">
                    <Grid Margin="0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Customer Name:" VerticalAlignment="Center" FontWeight="SemiBold" Margin="0,10,0,10"/>
                        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding CustomerName}" Style="{StaticResource MaterialDesignOutlinedTextBox}" FontWeight="Bold" Margin="0,5"/>

                        <TextBlock Grid.Row="1" Grid.Column="0" Text="Address:" VerticalAlignment="Center" FontWeight="SemiBold" Margin="0,10,0,10"/>
                        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Address}" Style="{StaticResource MaterialDesignOutlinedTextBox}" Margin="0,5"/>

                        <TextBlock Grid.Row="2" Grid.Column="0" Text="Phone:" VerticalAlignment="Center" FontWeight="SemiBold" Margin="0,10,0,0"/>
                        <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding Mobile}" Style="{StaticResource MaterialDesignOutlinedTextBox}" Margin="0,5,0,0"/>
                    </Grid>
                </GroupBox>

                <!-- Right Card: Quotation Details -->
                <GroupBox Grid.Column="2" Header="Quotation Details" Style="{DynamicResource MaterialDesignCardGroupBox}" materialDesign:ColorZoneAssist.Mode="Standard">
                    <Grid Margin="0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Quotation Date:" VerticalAlignment="Center" FontWeight="SemiBold" Margin="0,10,0,10"/>
                        <DatePicker Grid.Row="0" Grid.Column="1" SelectedDate="{Binding QuotationDate}" Style="{StaticResource MaterialDesignOutlinedDatePicker}" Margin="0,5"/>

                        <TextBlock Grid.Row="1" Grid.Column="0" Text="Valid Until:" VerticalAlignment="Center" FontWeight="SemiBold" Margin="0,10,0,0"/>
                        <DatePicker Grid.Row="1" Grid.Column="1" SelectedDate="{Binding ValidUntil}" Style="{StaticResource MaterialDesignOutlinedDatePicker}" Margin="0,5,0,0"/>
                    </Grid>
                </GroupBox>
            </Grid>

            <!-- DATAGRID: Select Products -->
            <GroupBox Grid.Row="1" Header="Select Products" Style="{DynamicResource MaterialDesignCardGroupBox}" materialDesign:ColorZoneAssist.Mode="Standard" Margin="0,20,0,0">
                <StackPanel>
                    <DataGrid ItemsSource="{Binding QuotationItems}" AutoGenerateColumns="False" CanUserAddRows="False" SelectionMode="Single"
                              materialDesign:DataGridAssist.CellPadding="15 10" materialDesign:DataGridAssist.ColumnHeaderPadding="15 10" Margin="0,10">
                        <DataGrid.Columns>
                            <!-- Single-click editable columns -->
                            <DataGridTemplateColumn Header="Component" Width="200">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBox Text="{Binding Component, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource MaterialDesignTextBox}" BorderThickness="0" Background="Transparent" FontWeight="SemiBold"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <DataGridTemplateColumn Header="Description / Specification" Width="*">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBox Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource MaterialDesignTextBox}" BorderThickness="0" Background="Transparent" TextWrapping="Wrap"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            
                            <DataGridTemplateColumn Header="Qty" Width="100">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBox Text="{Binding Quantity, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource MaterialDesignTextBox}" BorderThickness="0" Background="Transparent" materialDesign:HintAssist.Hint="e.g. 1 Set"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <DataGridTemplateColumn Header="Brand" Width="100">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBox Text="{Binding Brand, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource MaterialDesignTextBox}" BorderThickness="0" Background="Transparent"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>

                    <Button Content="Add Product" Command="{Binding AddProductCommand}" Style="{StaticResource MaterialDesignOutlinedButton}" HorizontalAlignment="Left" Margin="0,10,0,0">
                        <Button.ContentTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="Plus" VerticalAlignment="Center" Margin="0,0,5,0"/>
                                    <TextBlock Text="Add Product" VerticalAlignment="Center"/>
                                </StackPanel>
                            </DataTemplate>
                        </Button.ContentTemplate>
                    </Button>
                </StackPanel>
            </GroupBox>

            <!-- BOTTOM SECTION: Cost Summary & Actions -->
            <Grid Grid.Row="2" Margin="0,20,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Cost Summary (Left) -->
                <StackPanel Grid.Column="0" HorizontalAlignment="Left" MinWidth="400">
                    <Grid Margin="0,5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="Amount (Total Cost):" FontSize="16" VerticalAlignment="Center" Margin="0,0,30,0"/>
                        <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                            <TextBlock Text="₹ " FontSize="16" VerticalAlignment="Center" Margin="0,0,5,0"/>
                            <TextBox Text="{Binding TotalSystemCost, UpdateSourceTrigger=PropertyChanged, StringFormat='N2'}" Style="{StaticResource MaterialDesignTextBox}" FontSize="16" Width="100"/>
                        </StackPanel>
                    </Grid>
                    
                    <Separator Margin="0,10"/>
                    
                    <Grid Margin="0,5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="Less: Central Govt. Subsidy (PM Surya Ghar Yojana):" FontSize="16" VerticalAlignment="Center" Margin="0,0,30,0" Foreground="Green"/>
                        <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                            <TextBlock Text="- ₹ " FontSize="16" VerticalAlignment="Center" Margin="0,0,5,0" Foreground="Green"/>
                            <TextBox Text="{Binding Subsidy, UpdateSourceTrigger=PropertyChanged, StringFormat='N2'}" Style="{StaticResource MaterialDesignTextBox}" FontSize="16" Width="100"/>
                        </StackPanel>
                    </Grid>

                    <Separator Margin="0,10"/>

                    <Grid Margin="0,5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="Net Payable Amount:" FontWeight="Bold" FontSize="20" Foreground="{DynamicResource PrimaryHueDarkBrush}"/>
                        <TextBlock Grid.Column="1" Text="{Binding NetPayable, StringFormat='₹ {0:N2}'}" HorizontalAlignment="Right" FontWeight="Bold" FontSize="22" Foreground="{DynamicResource PrimaryHueDarkBrush}"/>
                    </Grid>
                    <Separator Margin="0,5,0,0" Height="2" Background="{DynamicResource PrimaryHueDarkBrush}"/>

                    <Grid Margin="0,15,0,5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="Overall GST %:" FontSize="14" Foreground="Gray" VerticalAlignment="Center" Margin="0,0,30,0"/>
                        <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                            <TextBox Text="{Binding OverallGstPercentage, UpdateSourceTrigger=PropertyChanged, StringFormat='N0'}" Style="{StaticResource MaterialDesignTextBox}" Foreground="Gray" FontSize="14" Width="50" HorizontalAlignment="Right"/>
                            <TextBlock Text=" %" FontSize="14" Foreground="Gray" VerticalAlignment="Center" Margin="5,0,0,0"/>
                        </StackPanel>
                    </Grid>

                    <Grid Margin="0,5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="Total CGST:" FontSize="14" Foreground="Gray" VerticalAlignment="Center" Margin="0,0,30,0"/>
                        <TextBlock Grid.Column="1" Text="{Binding TotalCGST, StringFormat='₹ {0:N2}'}" Foreground="Gray" HorizontalAlignment="Right" FontSize="14" VerticalAlignment="Center"/>
                    </Grid>

                    <Grid Margin="0,5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="Total SGST:" FontSize="14" Foreground="Gray" VerticalAlignment="Center" Margin="0,0,30,0"/>
                        <TextBlock Grid.Column="1" Text="{Binding TotalSGST, StringFormat='₹ {0:N2}'}" Foreground="Gray" HorizontalAlignment="Right" FontSize="14" VerticalAlignment="Center"/>
                    </Grid>
                    

                </StackPanel>

                <!-- Action Buttons (Right) -->
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Bottom">
                    <Button Content="Generate Quotation" Command="{Binding GenerateQuotationCommand}" 
                            Background="#388E3C" BorderBrush="#388E3C" Foreground="White"
                            FontSize="16" FontWeight="Bold" Height="50" Padding="20,0" Margin="0,0,15,0"/>
                    
                    <Button Content="Print Quotation" Command="{Binding ExportPdfCommand}" 
                            Background="#1976D2" BorderBrush="#1976D2" Foreground="White"
                            FontSize="16" FontWeight="Bold" Height="50" Padding="20,0"/>
                </StackPanel>
            </Grid>
        </Grid>
    </ScrollViewer>
</UserControl>
